<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example1.mapper.UserMapper">
    <insert id="insert" parameterType="com.example1.entity.User">
        insert into user (username, password, portrait, `role`, phone, salary, bonus)
        values(#{username}, #{password}, #{portrait}, #{role}, #{phone}, #{salary}, #{bonus})
    </insert>

    <delete id="deleteById" parameterType="int">
       delete from user where user_id = #{id}
    </delete>

    <update id="update" parameterType="com.example1.entity.User">
        update user set username = #{username}, password = #{password},
                        portrait = #{portrait}, `role` = #{role},
                        phone = #{phone}, salary = #{salary}, bonus = #{bonus}
    </update>

    <select id="selectById" resultType="com.example1.entity.User">
        select * from user where user_id = id
    </select>

    <select id="listAll" resultType="com.example1.entity.User">
        select * from user;
    </select>

    <select id="fuzzySearchByName" resultType="com.example1.entity.User">
        select * from user
        where username like '%#{part}%'
    </select>

    <select id="listByRole" resultType="com.example1.entity.User">
        select * from user where role = #{role}
    </select>
</mapper>